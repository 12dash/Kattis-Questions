#include <stdio.h>
#include <stdlib.h>

int main()
{
    int k,q,r,b,kn,p;
    int temp = scanf("%d %d %d %d %d %d",&k,&q,&r,&b,&kn,&p);

        temp = 1-k;
        printf("%d ", temp);

        temp = 1-q;
        printf("%d ", temp);

        temp = 2-r;
        printf("%d ", temp);

        temp = 2-b;
        printf("%d ", temp);
    temp = 2-kn;
        printf("%d ", temp);
    temp = 8-p;
        printf("%d ", temp);

    return 0;
}
